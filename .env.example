# LI.FI API Configuration
LIFI_API_KEY=your-lifi-api-key-here

# Optional: API Base URL (defaults to https://li.quest)
# LIFI_API_BASE_URL=https://li.quest

# Optional: Request timeout in milliseconds (defaults to 30000)
# LIFI_REQUEST_TIMEOUT=30000

# Optional: Cache TTL in milliseconds (defaults to 60000)
# LIFI_CACHE_TTL=60000

# Optional: Rate limiting (requests per minute, defaults to 100)
# LIFI_RATE_LIMIT=100

# Optional: Enable debug logging (defaults to false)
# LIFI_DEBUG=false

# Optional: Maximum retry attempts (defaults to 3)
# LIFI_MAX_RETRIES=3

# Optional: Retry delay multiplier in milliseconds (defaults to 1000)
# LIFI_RETRY_DELAY=1000
